---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: eso-test
spec:
  refreshInterval: 1m           
  secretStoreRef:
    kind: SecretStore
    name: gcp-store
  target:
    name: eso-test
    creationPolicy: Owner
  data:
  - secretKey: test
    remoteRef:
      key: test