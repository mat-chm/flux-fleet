---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: eso-prod
  namespace: external-secrets
spec:
  refreshInterval: 1m           
  secretStoreRef:
    kind: SecretStore
    name: gcp-store
  target:
    name: gcp-store-prod
    creationPolicy: Owner
  data:
  - secretKey: test
    remoteRef:
      key: test